<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css"
        integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
    <style>
        .sizeContainer{
            text-align:right;
            margin-left:60%;
            width:500px;
        }
    </style>
    <title>עורך מאמרים</title>
</head>
<body>
    <div  class="sizeContainer">
        <h1 class="mb-4">מאמרים - נשים אתיופיות מעצימות</h1>
        <a href="/articles/new" class="btn btn-success"> מאמרים חדשים</a>

    <% articles.forEach((article => { %>
        <div class="card mt-4">
            <div class="card-body">
            <h4 class="card-title"><%= article.title %></h4>
            <div class="card-subtitle text-muted mb-2">
                <%= article.createdAt.toLocaleDateString() %>
            </div>
            <div class="card-text mb-2">
                <%= article.description %>
            </div>
            <a href="articles/<%= article.slug %>" class="btn btn-primary">
            קרא עוד</a>
            <form action="/articles/<%= article.id%>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger"> מחיקה</button>
            </form>
            </div>
        </div>
        <% })) %>
            
    </div>

   
</body>
</html>